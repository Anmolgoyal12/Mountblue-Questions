#include <stdio.h>

int main()
{
    int add=0;
    int number;
     scanf("%d",&number);
    int arr[number];
    for(int i=0;i<number;i++)
    {
        scanf("%d",&arr[i]);
        add=add+arr[i];
    }
    printf("%d",add);
    return 0;
}
